import styled from 'styled-components';
import tw from 'twin.macro';

export const SectionContainer = styled.main`
  ${tw`flex flex-col justify-center items-center h-screen space-y-10 pb-20 sm:pb-36`}
`;
